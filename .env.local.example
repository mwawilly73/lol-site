# .env.local.example
# Active l'utilisation du CDN Data Dragon (Riot).
# 1 = ON (CDN) | 0 = OFF (retour aux assets locaux)
USE_DDRAGON=1

# (Optionnel) Figer une version spécifique de Data Dragon.
# Laisser vide = prendre automatiquement la dernière.
# Exemple de version: 15.16.1
DD_VERSION=
